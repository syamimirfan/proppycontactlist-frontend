import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'proppycontactlist.frontend',
  appName: 'proppycontactlist-frontend',
  webDir: 'www'
};

export default config;
